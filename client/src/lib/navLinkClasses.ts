/**
 * Active nav styles for TanStack Router `Link` `activeProps.className`.
 * Uses design tokens (`--accent`, etc.) so light / dark / auto themes stay correct.
 */
export const headerNavLinkActiveClass =
	"bg-accent text-accent-foreground font-medium hover:bg-accent hover:text-accent-foreground";
